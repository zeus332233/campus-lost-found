<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UserMapper">
    <select id="selectById" resultType="com.example.demo.model.User">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <!-- 添加用户名查询语句 -->
    <select id="selectByUsername" resultType="com.example.demo.model.User">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <!-- 添加用户插入语句 -->
    <insert id="insert" parameterType="com.example.demo.model.User">
        INSERT INTO user (username, password, phone, email, avatar, created_at, updated_at)
        VALUES (#{username}, #{password}, #{phone}, #{email}, #{avatar}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="updateById">
        UPDATE user
        <set>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="password != null">password = #{password},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
        </set>
        WHERE id = #{id}
    </update>
</mapper>